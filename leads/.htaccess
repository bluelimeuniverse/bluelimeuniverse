<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # API proxy rules
  RewriteRule ^/api/(.*)$ /api/$1 [P]

  # SPA routing
  RewriteCond %{REQUEST_URI} !^/api
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule . /index.html [L]
</IfModule>
